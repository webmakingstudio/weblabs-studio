name: "Deploy to Production (DISABLED)"
on:
  workflow_dispatch: # Solo ejecución manual
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: "Workflow Disabled"
      run: |
        echo "🚫 Este workflow está desactivado"
        echo "Para hacer deployment, usa Vercel Dashboard manualmente"
        echo "Para reactivar, descomenta las líneas en el archivo"
